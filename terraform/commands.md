# Terraform commands
```
terraform init
terraform validate
terraform plan
terraform apply
terraform destroy

terraform fmt # Сканирование файлов tf и приведение их к каноническому (удобочитаемому) виду.
```
